name: CI

on:
  push:
    branches:
      - main
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.repository }}-${{ github.ref_name }}
  cancel-in-progress: false

jobs:
  test:
    name: Test
    runs-on: codebuild-KenFirebreakCodeBuildRunner-${{ github.run_id }}-${{ github.run_attempt }}
    steps:
      - name: Always true
        run: echo "runs"